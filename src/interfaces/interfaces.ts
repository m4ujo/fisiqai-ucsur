export interface IPathnameInfo {
  title: string;
  url: string;
  description?: string;
}

export interface ITopic {
  id: number;
  title: string;
  name: string;
}

export interface IMarkdownContent {
  file: string;
  content: string;
}